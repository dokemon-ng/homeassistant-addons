{
  "name": "Dokemon Agent",
  "version": "1.6.0b",
  "slug": "dokemon_agent",
  "description": "Connects to a Dokemon server",
  "arch": ["amd64", "armv7", "aarch64"],
  "image": "javastraat/dokemon-agent:latest",
  "startup": "application",
  "boot": "auto",
  "init": false,
  "ports": {},
  "volumes": {
    "docker_sock": "/var/run/docker.sock"
  },
  "options": {
    "server_url": "http://192.168.2.203:9090",
    "token": ""
  },
  "schema": {
    "server_url": "str",
    "token": "str"
  },
  "environment": {
    "SERVER_URL": "{{ options.server_url }}",
    "TOKEN": "{{ options.token }}"
  }
}
